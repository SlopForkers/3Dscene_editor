#pragma once

// Small shared helpers for the editor UI translation units
// (app_ui.cpp, app_panels.cpp).

// Display name for a Terrain::BrushParams::Type value.
const char* brushTypeName(int t);
// Display name for a Terrain::BrushParams::Falloff value.
const char* falloffName(int f);
